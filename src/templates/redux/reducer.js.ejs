const initState = {
    // Your State variables
}

export default const <%=props.name%>Reducer = (state = initState, action ) => {
    switch (action.type){
        default:
            return state;
    }
}
